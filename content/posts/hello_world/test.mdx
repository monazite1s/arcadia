---
title: "Markdown测试文件"
date: "2025-11-25"
tags: ["Markdown", "测试", "Next.js", "MDX", "组件开发"]
excerpt: "Markdown测试文件"
series: "Markdown测试"
---

## 📄 完整的 Markdown 语法测试文件 (复制即可用)

# 博客标题 (H1)

这是一个标准的段落，用来测试基本的文本渲染。确保字体、行高和颜色正常显示。

## 二级标题 (H2) - 核心内容

### 三级标题 (H3) - 样式测试

#### 四级标题 (H4)

这是**粗体文本**，这是*斜体文本*，这是**_粗斜体文本_**。
使用两个波浪线标记~~删除线文本~~。

> 这是一个块级引用。
> 它常用于强调或引用外部文献。

## 列表 (Lists)

### 无序列表 (Unordered List)

- 列表项一
    - 嵌套列表 A
    - 嵌套列表 B
- 列表项二
- 列表项三

### 有序列表 (Ordered List)

1.  第一步：准备数据
2.  第二步：处理逻辑
3.  第三步：完成渲染

### 任务列表 (Task Lists - GFM 扩展)

- [x] 完成核心组件
- [x] 设置数据库连接
- [ ] 编写测试用例

## 代码与语法高亮

这是一个行内代码 `const variable = 10;` 的示例。

### 代码块 (Code Block - 带有语法高亮)

请测试您的代码高亮效果是否正常（例如使用 Prism 或 Shiki）。

```typescript
// 示例：TypeScript 代码
interface Post {
    id: number;
    title: string;
    isPublished: boolean;
}

const fetchPosts = async (count: number): Promise<Post[]> => {
    console.log(`Fetching ${count} posts...`);
    return [];
};
```

## 表格 (Tables - GFM 扩展)

请检查表格的边框、对齐方式是否正确渲染。

| 属性           | 类型     | 描述                 |
| :------------- | :------- | :------------------- |
| **Name**       | `String` | 文章标题（左对齐）   |
| **Slug**       | `String` | URL 路径（居中对齐） |
| **Revalidate** | `Number` | ISR 周期（右对齐）   |

## 链接与图片

### 链接 (Links)

这是一个标准的 [外部链接到 Google](https://www.google.com)。

### 图片 (Images)

请注意，您需要替换为有效的图片 URL 才能看到效果。

## HTML 实体与水平线

您可以使用 HTML 实体，例如：© 版权所有 2025。

这是一条水平线，用于分隔内容。

## MDX 扩展区（仅限 .mdx 文件）

请确保您的 MDX 处理器能够正确地将下面的组件嵌入到 Markdown 内容流中。

<h1> 这是一个测试用的 React 组件，确保它在 Markdown 中能够被正确解析和渲染。</h1>
